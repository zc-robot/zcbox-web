# Allow Manual Control With Stale Fleet State

Manual Control remains available when a selected robot is disconnected from Fleet State or only has Last Known State because operators may still need to send direct intervention commands through an available robot command path. Task dispatch still requires fresh fleet state, and the UI must keep the disconnected or stale state visible so operators understand that command feedback may be incomplete.
